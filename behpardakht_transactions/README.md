# افزونه مدیریت تراکنش‌های بهپرداخت ملت برای WHMCS

![Version](https://img.shields.io/badge/version-2.0-orange)
![WHMCS](https://img.shields.io/badge/WHMCS-8.11%2B-blue)
![PHP](https://img.shields.io/badge/PHP-7.4%2B-purple)
![License](https://img.shields.io/badge/license-MIT-green)

افزونه حرفه‌ای و بهینه شده برای نمایش و مدیریت تراکنش‌های درگاه پرداخت به‌پرداخت ملت در پنل مدیریت WHMCS.

## ✨ ویژگی‌ها

### 🎨 رابط کاربری
- **طراحی مدرن و مینیمال** - سازگار با تم پیش‌فرض WHMCS
- **رسپانسیو کامل** - قابل استفاده در موبایل، تبلت و دسکتاپ
- **RTL و فارسی** - پشتیبانی کامل از زبان فارسی
- **Dark Mode** - پشتیبانی خودکار از حالت تاریک
- **انیمیشن‌های نرم** - تجربه کاربری بهتر

### 🔍 جستجو و فیلتر
- جستجوی پیشرفته بر اساس:
  - شماره فاکتور
  - شماره سفارش
  - شماره تراکنش (ref_id)
- فیلتر بر اساس وضعیت (موفق، در انتظار، ناموفق)
- فیلتر بر اساس بازه تاریخی (از تاریخ - تا تاریخ)
- مرتب‌سازی بر اساس تمام ستون‌ها

### 📊 نمایش اطلاعات
- نمایش جزئیات کامل تراکنش‌ها
- نمایش اطلاعات مشتری با لینک مستقیم
- نمایش شماره فاکتور با لینک مستقیم
- نمایش مبلغ به ریال و تومان
- رنگ‌بندی سطرها بر اساس وضعیت تراکنش
- صفحه‌بندی هوشمند

### 📈 آمار و گزارش
- نمایش آمار کلی در Sidebar:
  - تعداد کل تراکنش‌ها
  - تعداد تراکنش‌های موفق
  - تعداد تراکنش‌های در انتظار
  - تعداد تراکنش‌های ناموفق
  - مجموع درآمد از تراکنش‌های موفق
- خروجی اکسل از تراکنش‌ها با حفظ فیلترها

### 🛡️ امنیت
- محافظت در برابر SQL Injection
- محافظت در برابر XSS
- اعتبارسنجی تمام ورودی‌ها
- استفاده از Prepared Statements
- Whitelist برای فیلتر وضعیت

### ⚡ بهینه‌سازی
- Query های بهینه شده
- دریافت اطلاعات مشتری به صورت یکجا (به جای query های مجزا)
- کش کردن استایل‌ها با versioning
- محدودیت تعداد نتایج برای جلوگیری از مصرف زیاد منابع

## 📋 پیش‌نیازها

- WHMCS نسخه 8.11 یا بالاتر
- PHP نسخه 7.4 یا بالاتر
- افزونه درگاه پرداخت بهپرداخت ملت که جدول `mod_behpardakht_transactions` را ایجاد کرده باشد

## 🚀 نصب

### مرحله 1: آپلود فایل‌ها

فولدر `behpardakht_transactions` را در مسیر زیر کپی کنید:

```
/path/to/whmcs/modules/addons/
```

ساختار نهایی باید به شکل زیر باشد:

```
modules/
  └── addons/
      └── behpardakht_transactions/
          ├── behpardakht_transactions.php
          ├── templates/
          │   └── admin.tpl
          └── css/
              └── style.css
```

### مرحله 2: فعالسازی افزونه

1. وارد پنل مدیریت WHMCS شوید
2. به `Setup` → `Addon Modules` بروید
3. افزونه **مدیریت تراکنش‌های بهپرداخت ملت** را پیدا کنید
4. روی `Activate` کلیک کنید
5. دسترسی‌های مورد نظر را به گروه‌های مدیریتی بدهید

### مرحله 3: پیکربندی

در صفحه تنظیمات افزونه می‌توانید موارد زیر را تنظیم کنید:

- **تعداد رکورد در هر صفحه**: تعداد تراکنش‌ها در هر صفحه (پیشنهادی: 25)
- **فعالسازی خروجی اکسل**: امکان دریافت خروجی اکسل از تراکنش‌ها

## 📖 نحوه استفاده

### دسترسی به افزونه

پس از نصب و فعالسازی، از منوی `Addons` → `مدیریت تراکنش‌های بهپرداخت ملت` وارد صفحه اصلی افزونه شوید.

### جستجو و فیلتر

1. در کادر جستجو می‌توانید شماره فاکتور، سفارش یا تراکنش را وارد کنید
2. از منوی کشویی وضعیت، نوع تراکنش مورد نظر را انتخاب کنید
3. بازه تاریخی مورد نظر را تعیین کنید
4. روی دکمه "جستجو" کلیک کنید

### مرتب‌سازی

روی عنوان هر ستون کلیک کنید تا تراکنش‌ها بر اساس آن ستون مرتب شوند.

### خروجی اکسل

1. فیلترهای مورد نظر را اعمال کنید
2. روی دکمه "خروجی اکسل" کلیک کنید
3. فایل اکسل با فرمت UTF-8 دانلود می‌شود

### مشاهده جزئیات

- روی نام مشتری کلیک کنید تا به صفحه مشتری منتقل شوید
- روی شماره فاکتور کلیک کنید تا به صفحه فاکتور منتقل شوید
- روی آیکون چشم کلیک کنید تا فاکتور را مشاهده کنید

## 🔧 تغییرات نسخه 2.0

### بهبودهای UI/UX
- ✅ طراحی کاملاً جدید و مدرن
- ✅ سازگاری کامل با تم WHMCS
- ✅ استفاده از CSS Variables برای سفارشی‌سازی آسان
- ✅ انیمیشن‌های نرم و حرفه‌ای
- ✅ پشتیبانی از Dark Mode
- ✅ رسپانسیو کامل برای موبایل و تبلت
- ✅ افزودن Sidebar با آمار تراکنش‌ها

### بهبودهای امنیتی
- ✅ محافظت در برابر SQL Injection
- ✅ محافظت در برابر XSS
- ✅ اعتبارسنجی کامل تمام ورودی‌ها
- ✅ استفاده از htmlspecialchars با ENT_QUOTES
- ✅ Whitelist برای فیلترهای وضعیت
- ✅ Regex validation برای تاریخ‌ها

### بهبودهای Performance
- ✅ حذف query های تکراری
- ✅ دریافت اطلاعات مشتری با یک JOIN
- ✅ استفاده از Capsule Query Builder
- ✅ محدودیت تعداد نتایج در خروجی اکسل
- ✅ کش کردن CSS با versioning

### بهبودهای کد
- ✅ حذف کدهای تکراری
- ✅ ساختار بهتر و خواناتر
- ✅ افزودن PHPDoc برای تمام توابع
- ✅ استفاده از تابع کمکی برای دریافت اطلاعات مشتری
- ✅ حذف فیلدهای ناموجود در جدول

### ویژگی‌های جدید
- ✅ کپی شماره تراکنش با یک کلیک
- ✅ Tooltip برای نمایش اطلاعات بیشتر
- ✅ نمایش آمار در Sidebar
- ✅ رنگ‌بندی سطرها بر اساس وضعیت
- ✅ نمایش مبلغ به ریال و تومان در خروجی اکسل

## 📁 ساختار فایل‌ها

```
behpardakht_transactions/
│
├── behpardakht_transactions.php    # فایل اصلی ماژول (362 خط)
│   ├── behpardakht_transactions_config()
│   ├── behpardakht_transactions_activate()
│   ├── behpardakht_transactions_deactivate()
│   ├── behpardakht_get_clients_info()
│   ├── behpardakht_transactions_output()
│   ├── behpardakht_export_excel()
│   └── behpardakht_transactions_sidebar()
│
├── templates/
│   └── admin.tpl                   # قالب صفحه اصلی (364 خط)
│       ├── سربرگ صفحه
│       ├── بخش فیلترها
│       ├── جدول تراکنش‌ها
│       ├── صفحه‌بندی
│       └── اسکریپت‌های JavaScript
│
└── css/
    └── style.css                   # فایل استایل (792 خط)
        ├── متغیرهای رنگی
        ├── سربرگ صفحه
        ├── بخش فیلترها
        ├── جدول
        ├── المان‌های UI
        ├── صفحه‌بندی
        ├── Responsive
        ├── Print Styles
        └── Dark Mode
```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌های اصلی

در فایل `css/style.css`، متغیرهای CSS را ویرایش کنید:

```css
:root {
    --bp-primary: #ff6600;        /* رنگ اصلی */
    --bp-primary-dark: #e55a00;   /* رنگ تیره */
    --bp-primary-light: #ff8533;  /* رنگ روشن */
    /* ... */
}
```

### تغییر تعداد پیش‌فرض رکوردها

در فایل `behpardakht_transactions.php`، خط 32:

```php
"Default" => "25",  // تعداد دلخواه را وارد کنید
```

### غیرفعال کردن Dark Mode

در فایل `css/style.css`، خطوط 759-780 را کامنت کنید:

```css
/* @media (prefers-color-scheme: dark) { ... } */
```

## 🐛 عیب‌یابی

### استایل‌ها اعمال نمی‌شود

1. Cache مرورگر را پاک کنید
2. در WHMCS به `Utilities` → `System` → `Clear System Cache` بروید
3. مطمئن شوید فایل CSS در مسیر صحیح قرار دارد

### تراکنش‌ها نمایش داده نمی‌شوند

1. مطمئن شوید جدول `mod_behpardakht_transactions` وجود دارد
2. از صفحه PHPMyAdmin ساختار جدول را بررسی کنید
3. مطمئن شوید فیلدهای زیر در جدول وجود دارند:
   - id
   - invoice_id
   - order_id
   - ref_id
   - amount_rial
   - status
   - created_at

### خطای "This file cannot be accessed directly"

فایل `behpardakht_transactions.php` را مستقیماً باز نکنید. فقط از طریق پنل WHMCS به افزونه دسترسی داشته باشید.

## 📝 سوالات متداول

### آیا این افزونه با WHMCS 9.x سازگار است؟
بله، افزونه با تمام نسخه‌های WHMCS 8.11+ و 9.x سازگار است.

### آیا می‌توانم ساختار جدول را تغییر دهم؟
بله، ولی باید کد PHP را متناسب با فیلدهای جدید ویرایش کنید.

### آیا می‌توانم از این افزونه با درگاه‌های دیگر استفاده کنم؟
بله، فقط کافی است نام جدول را در کد تغییر دهید.

## 🤝 مشارکت

برای گزارش باگ یا پیشنهاد بهبود، لطفاً یک Issue ایجاد کنید.

## 📄 مجوز

این افزونه تحت مجوز MIT منتشر شده است.

## 👨‍💻 توسعه‌دهنده

**Behpardakht Manager**
- نسخه: 2.0
- تاریخ: 2025

## 📞 پشتیبانی

برای پشتیبانی و سوالات:
- ایجاد Issue در GitHub
- ارسال ایمیل به تیم پشتیبانی

---

**نکته**: این افزونه صرفاً برای نمایش تراکنش‌ها طراحی شده و نیازی به ایجاد جدول ندارد. جدول باید توسط افزونه درگاه پرداخت ایجاد شده باشد.
